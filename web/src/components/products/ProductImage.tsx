"use client";

import { Control } from "react-hook-form";
import { ProductFormValues } from "@/validations/product-schema";

interface Props {
  control: Control<ProductFormValues>;
}

export default function ProductImage({ control }: Props) {
  return null;
}